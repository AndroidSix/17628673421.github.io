{
  "name": "17628673421.github.io static site",
  "user": "ubuntu",
  "install": "python3 --version && node --version",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000",
      "description": "Serves the static site at http://localhost:8000 for local preview (mirrors GitHub Pages static hosting)."
    }
  ],
  "ports": [
    {
      "name": "static site",
      "port": 8000
    }
  ]
}
